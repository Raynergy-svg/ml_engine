# Buddy Trading Learnings — Archive

Archived on 2026-03-23. Contains learnings from Phases 4-27.

---


### Phase 4: Production Hardening
- [2026-03-19] **feature/retrain_pipeline**: US-025. End-to-end retrain: RetrainTrigger writes retrain_request.json → continuous.py spawns background subprocess → scheduled_retrain.py runs → hold-out validation (>52%) → AccuracyGate reset for retrained pairs. Non-blocking (subprocess).
- [2026-03-19] **feature/walk_forward**: US-026. Walk-forward backtesting (5 folds, 60d train, 20d test). Model must pass >53% across all folds. Rejects random predictors (50.3%). Persists results per pair.
- [2026-03-19] **feature/real_mtf_data**: US-027. Multi-timeframe agent now fetches real H4 (4h cache) and D1 (24h cache) from OANDA API. Falls back to H1 synthesis on failure. metadata tracks data source.
- [2026-03-19] **feature/adaptive_risk**: US-028. Position sizing scales: 3 losses=0.70x, 5 losses=0.50x floor. Win streak recovery. 48h time decay toward baseline. Wired into position_sizing.py and learning loop.
- [2026-03-19] **feature/feature_importance**: US-029. Permutation importance analysis per pair. Identifies signal vs noise features. get_prune_list() returns noise features (<0.01 importance) for retraining.
- [2026-03-19] **feature/trade_clustering**: US-030. Clusters trades by (pair, direction, regime, session, conf_band). 3+ losses in cluster → -10% penalty. 3+ wins → +5% boost. Wired into engine.py after backtest gate.
- [2026-03-19] **arch/phase4_complete**: Phase 4 (US-025 through US-030) all 6 stories complete. 20+ automation modules active.

### Critical Bug Fixes (Post-Phase 4 Audit)
- [2026-03-19] **fix/rr_ratio_default**: R:R ratio gate default was 1.0 in ScannerConfig but trading rules require 1.2. Fixed default to 1.2.
- [2026-03-19] **fix/shadow_log_rotation**: shadow_predictions.jsonl had no rotation. Added 10MB rotation.
- [2026-03-19] **fix/scan_timeout**: ThreadPoolExecutor in engine.py scan had no timeout. Added 300s + 60s per-future timeout.
- [2026-03-19] **feature/automated_backup**: scripts/backup_state.py backs up trade_journal, agent_weights, pair_accuracy, state.json.
- [2026-03-19] **validation/system_audit_complete**: 14/14 modules import. Agent team fully functional.

### Phase 5 — Critical Risk Controls & Operational Resilience
- [2026-03-20] **fix/position_sizing_breach**: Live trading exposed 135% NAV risk on single trade. Added _check_projected_portfolio_risk() in execution.py. Non-negotiable safety gate.
- [2026-03-20] **fix/agent_consensus_gate**: 1/3 agent consensus was executing trades. Added hard minimum gate: min_agent_consensus_ratio (default 0.25 = 3/12 agents).
- [2026-03-20] **feature/degraded_ensemble**: When models missing, confidence *= available/expected. Prevents overconfident signals from incomplete model stacks.
- [2026-03-20] **audit/lazy_imports_clean**: All __init__.py files verified clean. CLI boots without heavy ML deps.
- [2026-03-20] **feature/validator_self_test**: validate_deployment.py --self-test with --auto-fix for agent weights and journal fields.
- [2026-03-20] **feature/status_dashboard**: scripts/buddy_status.py shows live OANDA NAV, open trades, ensemble health, agent weights, risk scaler, blocked pairs.
- [2026-03-20] **arch/phase5_complete**: Phase 5 all 6 stories complete. System hardened against self-harm.

### Phase 6 — Execution Intelligence & Portfolio Protection
- [2026-03-20] **feature/per_profile_consensus**: US-037. Per-profile min_agent_consensus_ratio: conservative=0.42, balanced=0.33, aggressive=0.25, smart=0.33.
- [2026-03-20] **feature/trailing_stop**: US-038. ATR-based trailing stop: trail_distance = ATR × trailing_atr_multiplier (default 1.5). Only moves SL toward profit. Wired into continuous.py monitoring loop.
- [2026-03-20] **feature/correlation_execution_gate**: US-039. _check_correlation_exposure() in execution.py blocks trades when max_correlated_exposure (default 2) reached in same group. Last-resort gate before OANDA order.
- [2026-03-20] **feature/rl_replay_buffer**: US-040. _sync_trade_outcome_to_rl() appends (state, action, reward) to trained_data/rl_replay_buffer.jsonl. Reward = normalized P/L relative to risk (pnl_pips / sl_pips). Called automatically after every trade close detection.
- [2026-03-20] **cleanup/orphan_agents_removed**: US-041. src/scanner/agents.py (70KB orphan) renamed to .orphan_backup — all functionality lives in agents/_team.py. No import breaks. Pre-Phase-4 learnings archived to learnings-archive.md.
- [2026-03-20] **feature/journal_enrichment**: US-042. Outcome data now includes exit_price, exit_time, duration_minutes, regime_at_entry, regime_at_exit. Three summary stats: win_rate_by_pair(), avg_duration(), avg_rr_ratio().
- [2026-03-20] **arch/phase6_complete**: Phase 6 (US-037 through US-042) all 6 stories complete. 26 automation modules active. Trade quality improved: trailing stops, correlation enforcement, per-profile consensus, RL replay buffer, enriched journal.

### Phase 7: Execution Resilience & Operational Hardening
- [2026-03-20] **feature/data_freshness_gate**: US-043. execute_trade() checks analysis_context.scan_time age. If > max_data_age_seconds (default 60s), returns early with stale data error. Prevents executing on outdated signals.
- [2026-03-20] **feature/safe_json_io**: US-044. safe_json.py: _FileLock (fcntl), safe_json_read (corruption recovery from .bak), safe_json_write (temp+fsync+os.rename atomic), safe_jsonl_append (exclusive lock). Wired into execution.py journal writes and agents/_team.py weight saves.
- [2026-03-20] **feature/api_retry_circuit_breaker**: US-045. api_retry.py: CircuitBreaker (sliding window deque, 50% failure threshold, 60s cooldown). retry_api_call (exponential backoff 1s/3s/10s, auth 401/403 fail-fast). Wired into fetch_live_nav via _retry_oanda helper.
- [2026-03-20] **feature/order_rejection_recovery**: US-046. On OANDA order rejection, auto-downsize by rejection_downsize_factor (0.5) and retry up to max_order_attempts (2). Journal entry only after successful fill. fill_status field on ExecutionResult.
- [2026-03-20] **feature/slippage_tracking**: US-047. slippage_pips = (fill_price - expected_price) / pip_value, direction-adjusted. Journal enriched with slippage_pips and fill_price. execution_quality_summary() returns avg/max slippage, fill rate. High slippage logged to observations.
- [2026-03-20] **feature/config_extraction**: US-048. 9 hardcoded params extracted to ScannerConfig+ExecutionConfig: max_order_attempts, rejection_downsize_factor, high_slippage_alert_pips, fallback_tp_pips, fallback_atr_pips, min/max_lot_size, trailing_stop_breakeven/lock_pct. All wired through engine.py.
- [2026-03-20] **arch/phase7_complete**: Phase 7 (US-043 through US-048) all 6 stories complete. 31 automation modules active. Execution resilience hardened: stale data blocked, JSON corruption prevented, API failures handled gracefully, order rejections auto-recovered, slippage tracked, magic numbers eliminated.

### Phase 8: Advanced Intelligence & Self-Tuning
- [2026-03-20] **feature/regime_tracker**: US-049. RegimeTransitionTracker: 4x4 Markov transition matrix tracks LOW/NORMAL/HIGH/EXTREME state changes. Persists to trained_data/regime_transitions.json every 5 updates via safe_json. predict_next() returns probability distribution. get_regime_risk_modifier() returns 0.85 when P(EXTREME|current) >= 0.30. Wired into engine.py scan loop with Counter.most_common for dominant regime detection.
- [2026-03-20] **feature/regime_adaptive_atr**: US-050. Dynamic SL/TP multipliers by volatility regime: LOW(0.8x SL, 1.2x TP), NORMAL(1.0, 1.5), HIGH(1.1, 2.0), EXTREME(1.2, 2.8). Applied in engine.py _scan_pair before per-pair config overrides. Logged in journal regime section. Configurable via ScannerConfig.regime_atr_multipliers.
- [2026-03-20] **feature/session_snapshots**: US-051. SessionSnapshotManager saves agent_weights, pair_accuracies, regime_counts, trade stats to trained_data/session_snapshots/{timestamp}.json. blend_agent_weights() does EMA: 0.7*current + 0.3*avg(last 5 sessions). Wired into continuous.py watch loop exit for cross-session learning persistence.
- [2026-03-20] **feature/statistical_ab_promotion**: US-052. ModelManager.check_promotion_with_significance() uses Fisher exact test (scipy.stats), chi-squared fallback, normal approximation last resort. Criteria: delta > 0.05 AND p < 0.05 AND n >= 50. auto_promote_if_ready() logs to version_history.jsonl with full stats.
- [2026-03-20] **feature/spread_aware_execution**: US-053. _check_spread_conditions() fetches live OANDA bid/ask pricing, computes spread in pips, blocks execution if spread > max_spread_pips (default 3.0). Wired before SL/TP price calculation. spread_pips injected into analysis_context and logged in journal entry_record.
- [2026-03-20] **feature/group_momentum**: US-054. GroupMomentumAggregator maps 27 pairs to 8 currency groups. compute_group_momentum() averages confidence-weighted direction signals per currency (-1 to +1). evaluate_trade() blocks when alignment < -0.2 (conflicting with group direction), boosts +5% position when alignment > 0.5. Wired as pre-filter in engine.py execute_trades. Logged in journal entry_record.
- [2026-03-20] **arch/phase8_complete**: Phase 8 (US-049 through US-054) all 6 stories complete. 37 automation modules active. Bot intelligence upgraded: Markov regime prediction, adaptive ATR, cross-session learning, statistical model promotion, spread gating, cross-pair momentum aggregation. Validation: 47 PASS, 2 WARN (optional TF/XGB), 0 FAIL.

### Phase 9: Adaptive Learning & Feedback Loops
- [2026-03-21] **feature/agent_health_monitor**: US-055. AgentHealthMonitor tracks per-agent metrics (wins, losses, aligned/misaligned votes). Agents with win rate < 35% over 20+ trades get 2x selective decay rate. Wired into _team.py apply_weight_decay() with lazy import. Decay events audited to agent_decay_log.jsonl.
- [2026-03-21] **feature/predictive_sizing**: US-056. get_predictive_size_modifier() on RegimeTransitionTracker uses Markov predict_next() probabilities. P(HIGH)>0.3 → 0.85x, P(EXTREME)>0.3 → 0.70x, compounds, clamped to min 0.5. Wired into engine.py analysis_context and execution.py lot sizing. Forward-looking risk reduction before volatility hits.
- [2026-03-21] **feature/config_auto_rollback**: US-057. ConfigTuner.compare_session_performance() loads 3 recent snapshots via SessionSnapshotManager, compares win rates, identifies suspect config changes made after last snapshot. Auto-reverts when win rate drops >10%. Rollback events logged to config_rollback_log.jsonl. Prevents tuner from drifting into bad parameter space.
- [2026-03-21] **feature/dynamic_drawdown**: US-058. DynamicDrawdownManager computes drawdown severity (current_dd / max_dd). Three modes: protective (severity>0.6, tighten breakeven/lock), normal (0.3-0.6), aggressive (severity<0.3, widen for room to run). Interpolates smoothly between modes. Wired into execution.py _manage_trailing_stops.
- [2026-03-21] **feature/observation_expansion**: US-059. Added group_momentum and correlation_conflict categories to ObservationLog. log_group_momentum() captures blocks/boosts with alignment scores. log_correlation_conflict() captures diversification filter removals. get_observation_summary() returns category counts. Wired into engine.py execute_trades and scan correlation filter.
- [2026-03-21] **feature/online_rl**: US-060. OnlineWeightUpdater reads last 10 trades from journal, computes per-agent win/loss signals, applies micro-adjustments (clamped ±0.10) to _global weights every 5 scan cycles. Separate from full RL sync — tighter feedback loop during active trading. Audit logged to online_rl_updates.jsonl. Wired into continuous.py scan loop.
- [2026-03-21] **arch/phase9_complete**: Phase 9 (US-055 through US-060) all 6 stories complete. Feedback loops fully closed: stale agents decayed, regime predictions drive sizing, config tuner self-corrects, drawdown adapts trailing stops, observations expanded, online RL tightens weight updates. 21 automation modules active.

### Phase 10: Execution Intelligence & Causal Learning
- [2026-03-21] **feature/exit_reason**: US-061. _determine_exit_reason() classifies trade exits: tp_hit, sl_hit, trailing_stop, breakeven_stop, timeout, manual_close. Compares close_price against SL/TP with 2-pip tolerance. exit_reason field added to outcome_data in sync_closed_trades_rl. Enables causal RL — system can now distinguish "good rule bad luck" from "bad rule."
- [2026-03-21] **feature/pair_affinity**: US-062. PairAffinityTracker builds co-occurrence matrix from overlapping trades. get_affinity_score() returns -1 to 1 based on (co_wins - co_losses) / total. get_best_companions() recommends optimal pair combinations. Persists to pair_affinity_matrix.json. Enables portfolio optimization beyond simple correlation blocking.
- [2026-03-21] **feature/gate_health**: US-063. GateHealthTracker monitors per-gate rolling accuracy (50-prediction window). When gate accuracy drops below 50%, flags as degraded and recommends heuristic fallback. Fallback types defined per gate (momentum→adx_threshold, confidence→vote_threshold, etc.). Logs gate_degradation to observation_log. Prevents silent model drift.
- [2026-03-21] **feature/seasonality**: US-064. SeasonalityTracker extracts per-pair hour-of-day win rates from journal. get_hour_modifier() returns 0.70-1.30 confidence multiplier. Wired into engine.py analysis_context. Pairs with historical >60% win rate at current hour get boost; <40% get penalty. Persists to hour_seasonality.json.
- [2026-03-21] **feature/agent_attribution**: US-065. AgentHealthMonitor extended with per-pair metrics. record_outcome() now accepts pair for pair-specific tracking. get_pair_agent_matrix() returns {pair: {agent: {win_rate, trades}}}. get_pair_specific_decay_rates() applies 3x decay for agents with <30% win rate on specific pairs. Enables targeted agent retraining.
- [2026-03-21] **feature/macro_stress**: US-066. MacroStressDetector computes composite stress score from USD volatility, regime persistence, spread widening. Three tiers: mild (0.3-0.5 → 0.85x), severe (0.5-0.7 → 0.60x), extreme (>0.7 → 0.50x). No external API — derived from existing OANDA data. Wired into engine.py and execution.py. Logs macro_stress to observation_log.
- [2026-03-21] **arch/phase10_complete**: Phase 10 (US-061 through US-066) all 6 stories complete. 25 automation modules active. Execution intelligence upgraded: causal exit tracking, pair affinity portfolio optimization, gate degradation detection, hour-of-day seasonality, per-agent per-pair attribution, macro stress filtering. Validation: 48 checks, 46 PASS, 2 WARN, 0 FAIL.

### Phase 11: Integration Wiring & Self-Diagnostics
- [2026-03-21] **feature/exit_reason_learning**: US-067. extract_exit_reason_patterns() added to learning_engine.py. 5 pattern rules: SL-hit >60%, TP-hit >70%, trailing_stop outperforms TP by 1.5x, timeout >30%, breakeven >40%. Groups by exit_reason per pair from trade_journal_rl.json. Wired as Step 5b in orchestrator LEARN phase.
- [2026-03-21] **feature/agent_health_orchestrator**: US-068. AgentHealthMonitor wired into orchestrator Step 4b. record_outcome(agent_verdicts, trade_won, pair) called for every synced closed trade. agent_health_report included in OrchestrationResult. Closes the attribution feedback loop.
- [2026-03-21] **feature/regime_agent_toggle**: US-069. regime_disabled_agents added to ScannerConfig: EXTREME disables mean_reversion+session_timing, LOW disables volatility. _team.py evaluate() filters out disabled agents post-collection before vote tally. Skipped agents logged. Empty dict = backward compatible.
- [2026-03-21] **feature/macro_stress_orchestrator**: US-070. MacroStressDetector wired into orchestrator Step 2b. Extracts dominant regime via Counter.most_common, averages spread ratios from analyses, calls update(). stress_state (score, modifier, regime) added to OrchestrationResult.
- [2026-03-21] **feature/online_rl_orchestrator**: US-071. OnlineWeightUpdater wired into orchestrator Step 2c. maybe_update(cycle_count) called every cycle. Returns WeightAdjustment list serialized into online_rl_update. Runs independently from full RL sync — tighter feedback loop.
- [2026-03-21] **feature/system_diagnostics**: US-072. QAPipeline + GateHealthTracker + AgentHealthMonitor wired into _get_system_health_report(). Combines QA score (0-100 normalized to 0-1), gate degradation fraction, stale agent ratio into overall_score. Runs every 10 cycles. Warns when score < 0.6.
- [2026-03-21] **arch/phase11_complete**: Phase 11 (US-067 through US-072) all 6 stories complete. 25 automation modules now fully wired into orchestrator. Integration complete: exit patterns feed learnings, agent health drives decay, regime toggles agents, macro stress updates per-cycle, online RL micro-adjusts weights, system diagnostics report every 10 cycles. Validation: 10/10 modules import clean.

### Phase 12: Advanced Portfolio Optimization & Execution Intelligence
- [2026-03-20] **feature/hrp_rebalancer**: US-073. HRPOptimizer uses scipy hierarchical clustering + recursive bisection for continuous position weighting. Ledoit-Wolf shrinkage stabilizes correlation estimation for <50 samples. Replaces binary correlation blocking with proportional weights (correlated pairs get reduced weight, not removed). Wired into engine.py scan flow.
- [2026-03-20] **feature/microstructure_regime**: US-074. MicrostructureRegimeDetector tracks 3 signals: spread_ratio (current/baseline), spread_acceleration, bid_ask_imbalance. Combined into microstructure_score (50/30/20 weighted). Predicts regime 30-60s earlier than volatility alone. Uses H-L range as spread proxy since OANDA mid-candles lack bid/ask. Confidence lift +5-15% when micro aligns with vol regime.
- [2026-03-20] **feature/smart_execution**: US-075. SmartExecutionEngine with VWAP (U-shaped volume profile) and TWAP (equal slices) strategies. Adaptive retry reduces subsequent slices by 20% when slippage exceeds threshold. should_use_smart_execution() gates on position size ≥0.05 lots or ≥0.03 in volatile markets. Wired into execution.py as planning layer.
- [2026-03-20] **feature/graph_attention_consensus**: US-076. GraphAttentionConsensus added to _team.py with softmax attention mechanism. 4 specialization categories (momentum, regime, risk, execution) with regime-dependent alignment scoring. 60/40 blend with original weights. Falls back gracefully when disabled.
- [2026-03-20] **feature/dynamic_hedging**: US-077. DynamicHedgeManager auto-opens inverse positions during macro stress (modifier <0.70). Hedge = primary_lots * |correlation| * vol_ratio. Static FX correlations as fallback, live correlations when available. Auto-releases hedges when stress recedes (modifier >0.90). Wired into orchestrator Step 2d.
- [2026-03-20] **feature/observational_learning**: US-078. 5 synthetic agents (MomentumChaser, MeanReversionScalper, RiskAverse, MacroCyclical, NewsReactive) generate trade scenarios from historical candles. ObservationalLearner extracts regime_preference and feature_threshold patterns, maps synthetic→live agents via SYNTHETIC_TO_LIVE_MAP, blends at 20% synthetic / 80% live.
- [2026-03-20] **arch/phase12_complete**: Phase 12 (US-073 through US-078) all 6 stories complete. 31 automation modules. Advanced portfolio optimization: HRP replaces binary correlation blocking, microstructure signals for earlier regime detection, VWAP/TWAP execution slicing, graph-attention agent consensus, dynamic hedging during stress, observational learning from synthetic agents. Validation: 11/11 files pass py_compile.

### Phase 13: Signal Intelligence & Adaptive Calibration
- [2026-03-20] **feature/confidence_calibrator**: US-079. ConfidenceCalibrator uses Platt scaling (sklearn LogisticRegression) to transform raw model confidence into calibrated probabilities. Manual fallback via binned logit regression when sklearn unavailable. Auto-loads trade_journal_rl.json, needs 30+ trades to activate. Refits every 20 new trades. Persists to confidence_calibration.json. Wired into engine.py after microstructure lift.
- [2026-03-20] **feature/dynamic_sl_tp**: US-080. DynamicSLTPOptimizer applies 4 regime-conditioned rules: trend_widen (expand TP in strong trends), early_tighten (contract SL on low confidence), vol_relax (widen SL in high volatility), trailing_adaptive (regime-specific trailing params). Each rule has multiplier tables per regime (LOW/NORMAL/HIGH/EXTREME). Aggressiveness parameter scales all rules. R:R 1.2:1 minimum enforced post-optimization.
- [2026-03-20] **feature/multi_horizon_fusion**: US-081. MultiHorizonFusion uses Bayesian model averaging across M1/M5/H1/H4 timeframes. Per-TF Bayesian weights updated by inverse error rate every 50 trades. Agreement grading: A (all agree, +10%), B (3/4, +0%), C (2 or less, -15%). Primary M5 signal direct, H1/H4 derived from trend strength.
- [2026-03-20] **feature/trade_outcome_predictor**: US-082. TradeOutcomePredictor classifies open trade fate from 7 post-entry features (dist_to_sl_pct, dist_to_tp_pct, mae_pips, mfe_pips, bars_in_trade, momentum_since_entry, vol_ratio). GradientBoosting primary with logistic regression persistence. 4 actions: HOLD (>0.55), TIGHTEN_SL (0.40-0.55), TAKE_PARTIAL (high MFE+declining), EXIT_EARLY (<0.30).
- [2026-03-20] **feature/concept_drift**: US-083. ConceptDriftDetector monitors 3 signal streams via rolling z-score: prediction_error, agent_disagreement, microstructure_anomaly. 100-observation window, z>2.5 threshold. Severity: LOW (1 stream, 0.85x penalty), MEDIUM (2 streams, 0.70x), HIGH (3 streams, 0.50x). 20-observation warmup before activation. Drift history persisted.
- [2026-03-20] **feature/kelly_portfolio**: US-084. KellyPortfolioOptimizer computes portfolio-level position sizing. Full Kelly f*=(p*b-q)/b, Half-Kelly for production. VaR constraint: 15% NAV max at 95% confidence. Correlation-aware: static FX correlations reduce size for concentrated exposure (0.3-1.0 multiplier). Confidence-scaled. Replaces simple risk_pct in engine.py.
- [2026-03-20] **arch/phase13_complete**: Phase 13 (US-079 through US-084) all 6 stories complete. 37 automation modules. Signal intelligence: Platt scaling calibration, dynamic SL/TP adaptation, multi-horizon fusion, trade outcome prediction, concept drift detection, portfolio Kelly criterion. Validation: 8/8 files pass py_compile.

### Phase 14: Meta-Signals & Robustness
- [2026-03-20] **feature/ensemble_disagreement**: US-085. EnsembleDisagreementSignal computes prediction_variance, prediction_spread, and directional_agreement from TCN/Ridge/RF ensemble outputs. Rolling 50-observation history with z-score trend detection (z>1.5 = regime shift signal). Position multiplier 0.5-1.0 based on disagreement level. Wired into engine.py scan loop before multi-horizon fusion.
- [2026-03-20] **feature/position_timeout**: US-086. PositionTimeoutManager tracks open trades with regime-dependent time-decay. 5 decay schedules: trending (100 bars, 0.98/bar), mean_revert (30 bars, 0.92), crisis (10 bars, 0.85), choppy (40, 0.95), normal (50, 0.96). 5-bar grace period. TP tightening after regime-specific start point. Force-exit on max_bars or decay < threshold. Tick-based architecture for orchestrator.
- [2026-03-20] **feature/lead_lag_detector**: US-087. LeadLagDetector uses normalized cross-correlation at +-10 lag offsets to build NxN pair lead-lag matrix. Matrix recomputes every 100 scans. get_lagging_signals() predicts moves on lagging pairs with timing offsets. Confidence boost up to +5% when current pair is corroborated by a leading pair.
- [2026-03-20] **feature/feature_attention**: US-088. FeatureAttentionLayer applies softmax attention over 10 technical features conditioned on volatility regime. 4 regime bias profiles (LOW/NORMAL/HIGH/EXTREME) with distinct feature priorities. Biases learn from trade outcomes (0.02 LR). Attention entropy logged for interpretability. Wired before multi-horizon fusion.
- [2026-03-20] **feature/adversarial_trainer**: US-089. AdversarialTrainer generates 3 adversarial market scenarios: stop_hunt (volatility spike + label flip), spoof (inflated momentum + partial flip), correlation_trap (decorrelate normally paired features). train_robust() iterates N rounds combining clean+adversarial data. Robustness score: clean_acc/adv_acc ratio. Training-time component for model retraining pipeline.
- [2026-03-20] **feature/regime_reward**: US-090. RegimeAwareReward replaces simple PnL reward with regime-conditioned shaping. 5 profiles: trending (momentum+duration bonus), mean_revert (quick exit bonus), crisis (preservation bonus, 3x drawdown penalty), choppy (slight hold penalty), normal (mild momentum bonus). Anti-reward-hacking via rolling 50-trade PnL-reward correlation — alerts when < 0.50.
- [2026-03-20] **arch/phase14_complete**: Phase 14 (US-085 through US-090) all 6 stories complete. 43 automation modules. Meta-signals: ensemble disagreement, position timeout, lead-lag detection, feature attention. Robustness: adversarial training, regime-aware reward shaping. Validation: 8/8 files pass py_compile.

### Phase 15: Intelligence Scaling & Explainability
- [2026-03-22] **feature/adaptive_lr**: US-091. AdaptiveLRScheduler replaces fixed RL learning rate with 3-phase cosine-annealing: warmup (linear ramp 0.001→target over 20 trades), cosine_decay (target→min over 200 trades), plateau_restart (mini-cycle on stall detection via rolling window comparison). Wired into online_rl.py — replaces fixed `self.learning_rate` with scheduled lookup.
- [2026-03-22] **feature/market_impact**: US-092. MarketImpactModel implements simplified Almgren-Chriss: permanent impact = a*(size/volume)^1.5, temporary = 0.5*spread*(size/volume)^0.5. 4 regime coefficient profiles (LOW/NORMAL/HIGH/EXTREME) with escalating permanent_coeff and temporary_mult. Position reduced when slippage > 1.5 pips. Wired into engine.py after Kelly sizing.
- [2026-03-22] **feature/model_bandit**: US-093. ModelBanditSelector uses EXP3 multi-armed bandit for TCN/Ridge/RF model routing. Importance-weighted reward updates (r_hat = r/p) with tanh-squashed PnL normalization. Gamma decay (0.999) with 0.02 floor prevents over-exploitation. Weight normalization keeps sum ≈ K. Persists full arm statistics.
- [2026-03-22] **feature/trade_explainer**: US-094. TradeExplainer computes permutation-based feature attributions with 10-feature directional consistency checks. Consistency score from sign-alignment of feature values vs expected direction. Entropy-based feature spread scoring (uniform = high explainability). Linear penalty up to 0.15 confidence when score < 0.40 threshold. Wired before Kelly sizing.
- [2026-03-22] **feature/causal_filter**: US-095. CausalSignalFilter implements OLS-based Granger F-test with multi-lag optimization (1-5 lags). Builds per-regime causal graphs. Wilson-Hilferty F-distribution approximation avoids scipy dependency. Signal consistency scoring via causal coefficient direction alignment.
- [2026-03-22] **feature/crisis_generator**: US-096. CrisisEventGenerator produces 3 synthetic crisis types: flash_crash (5-15% drop + partial recovery), volatility_spike (sinusoidal ATR ramp 2-10x), correlation_break (inverted returns on paired data). Includes replay_scenario() for simplified P/L simulation and batch generation for RL training augmentation.
- [2026-03-22] **arch/phase15_complete**: Phase 15 (US-091 through US-096) all 6 stories complete. 49 automation modules. Intelligence scaling: adaptive LR, market impact, model bandit. Explainability: trade explainer, causal filter. Training: crisis generator. Scan pipeline order: explainability → Kelly → market impact → execution. All 8 files pass py_compile.

### Phase 16: Adaptive Execution & Meta-Learning
- [2026-03-20] **feature/execution_quality_tracker**: US-097. ExecutionQualityTracker implements TCA feedback loop — tracks slippage, fill rates, timing per pair. Quadratic confidence penalty: high-confidence signals with poor historical execution get penalized more. PairQualityStats with avg_slippage, fill_rate, timing_score, cost_efficiency. Closes execution→agent learning gap.
- [2026-03-20] **feature/entropy_sizer**: US-098. EntropyPositionSizer measures Shannon entropy of 12-agent vote distribution (FOR/AGAINST/NEUTRAL). 4 regime-conditioned threshold profiles: LOW (tolerant, neutral=0.55), NORMAL (0.50), HIGH (strict, 0.40), EXTREME (very strict, 0.30). Size multiplier [0.30, 1.20]. Wired after market impact in scan pipeline.
- [2026-03-20] **feature/regime_broadcaster**: US-099. RegimeBroadcaster implements event-driven regime transition propagation. register_listener/broadcast_transition pattern with error-resilient notification. RegimeEvent dataclass with old/new regime, confidence, timestamp. Transition frequency tracking for regime-flip detection. Closes stale-regime-state problem.
- [2026-03-20] **feature/agent_accuracy_matrix**: US-100. AgentAccuracyMatrix tracks per-agent directional accuracy by regime and pair with rolling window. Dynamic weight multipliers: agents >60% accuracy get up to 1.15x boost, <45% get down to 0.80x penalty. MIN_SAMPLES=10 prevents premature weighting. get_best_agents() for top-N selection.
- [2026-03-20] **feature/pair_transfer**: US-101. PairTransferLearning uses union-find clustering to group correlated pairs into families (default: usd_majors, jpy_pairs, chf_pairs, cad_pairs, crosses). Transfer blends feature weights at ratio=0.30 from high-data to low-data pairs. Family confidence boost (max 0.10) when family accuracy > 0.50. auto_transfer() for batch operation.
- [2026-03-20] **feature/temporal_attention**: US-102. TemporalAttentionLayer replaces hardcoded lookback windows with learned attention logits (pre-softmax) over timeframes [5, 20, 50, 100]. Per-regime profiles. Gradient-free online learning: wins boost aligned timeframes, losses penalize. Softmax temperature=1.0, decay toward uniform (0.001) prevents over-specialization.
- [2026-03-20] **arch/phase16_complete**: Phase 16 (US-097 through US-102) all 6 stories complete. 55 automation modules. Strategic shift: closed feedback loops instead of adding standalone modules. Execution→agents (US-097), regime→all modules (US-099), agent accuracy per regime (US-100). Meta-learning: entropy sizing (US-098), pair transfer (US-101), temporal attention (US-102). All files pass py_compile.

### Phase 17: Signal Consumption & Feedback Loop Closure
- [2026-03-20] **feature/position_manager**: US-103. LivePositionManager wires trade_outcome_predictor into execution loop. Computes 7 post-entry features from live position context. Routes win_probability to HOLD/TIGHTEN_SL/TAKE_PARTIAL/EXIT_EARLY. Circuit breaker (5 max actions/trade), cooldown (5 bars between actions). Closes the prediction→action feedback loop.
- [2026-03-20] **feature/affinity_portfolio**: US-104. AffinityPortfolioSizer consumes pair_affinity_matrix.json (previously written but never read). Continuous position multiplier [0.50, 1.20] replaces binary correlation blocking. Conservative blend: 60% avg affinity + 40% max affinity. Enables nuanced portfolio diversification without hard cutoffs.
- [2026-03-20] **feature/execution_router**: US-105. ExecutionRouter bridges smart_execution and execute_trade(). Regime-conditioned lot thresholds (LOW=0.10 → EXTREME=0.03). TWAP slicing with adaptive slice reduction (0.80x) when slippage > 1.5 pips. Per-pair historical slippage tracking. Closes the smart_execution→live execution gap.
- [2026-03-20] **feature/model_router**: US-106. ModelRouter activates model_bandit routing. Selects primary (70% weight) + fallback (30%) model per pair/regime. Falls back to ALL_MODELS when bandit confidence < 0.40. Feeds tanh-squashed PnL reward back to bandit. Reduces compute by ~33% for routed trades.
- [2026-03-20] **feature/training_augmenter**: US-107. TrainingAugmenter injects synthetic scenarios into retrain datasets. 5 behavioral templates (momentum_chase, mean_reversion, risk_averse, crisis_adaptive, breakout_hunter). 60% chance of generating for underrepresented regimes. Synthetic weight = 0.50, ratio = 0.20. Bridges observational_learning→retrain pipeline.
- [2026-03-20] **feature/attention_feedback**: US-108. AttentionFeedbackLoop creates bidirectional flow between agent consensus quality and temporal_attention logits. Consensus-weighted alignment scoring per timeframe. Quality-driven logit updates (LR=0.03). Convergence detection: rolling std < 0.05 over 20-sample window.
- [2026-03-20] **arch/phase17_complete**: Phase 17 (US-103 through US-108) all 6 stories complete. 61 automation modules. Theme: signal consumption — wired 6 orphaned modules into active decision pipelines. No new signal generators created; instead closed every major feedback gap identified in codebase analysis. All files pass py_compile.

### Phase 18: Adaptive Quality & Optimization
- [2026-03-20] **feature/execution_quality_optimizer**: US-109. ExecutionQualityOptimizer profiles execution cost per model+regime combination. Cost score [0-1] from slippage (50%), fill_time (30%), fill_rate (20%). Recommends gate adjustments: high cost (>0.60) → raise confidence threshold, low cost (<0.25) → lower slightly. MAX_THRESHOLD_DELTA=0.05. Bridges execution_quality_tracker → gate tuning.
- [2026-03-20] **feature/threshold_optimizer**: US-110. ThresholdOptimizer auto-tunes confidence/momentum/rr_ratio gates based on rolling win rate vs 55% target. Above target → tighten (full LR), below target → loosen (half LR). Safety bounds enforced: confidence [0.40-0.85], momentum [0.10-0.60], rr_ratio [1.0-2.5]. ADJUSTMENT_LR=0.02. First module to close the outcome→gate configuration loop.
- [2026-03-20] **feature/dynamic_risk_allocator**: US-111. DynamicRiskAllocator adapts position sizing from P/L distribution. Three-factor: streak detection (3+ consecutive), P/L momentum (10-trade window), half-Kelly fraction upper bound. Combined 50/50 streak+momentum, capped by Kelly. Multiplier [0.50-1.30]. Replaces static base_risk_pct with adaptive sizing.
- [2026-03-20] **feature/model_calibration**: US-112. ModelCalibrationTracker builds reliability diagrams per model. 10-bin Expected Calibration Error (ECE). Overconfidence ratio: fraction of bins where predicted > actual. Flocking detection: agreement_ratio ≥0.90 at avg_confidence ≥0.65 triggers warning. Historical flock accuracy tracking. First module to audit model confidence quality.
- [2026-03-20] **feature/pair_regime_agent_matrix**: US-113. PairRegimeAgentMatrix extends agent_accuracy_matrix (US-100) with pair dimension. 3-way nested structure for pair×regime×agent cells. Accuracy-based weight multiplier [0.50-1.40]. Specialization report reveals which agents excel in which specific contexts. Min 8 samples per cell.
- [2026-03-20] **feature/signal_timing**: US-114. SignalTimingOptimizer tracks signal/entry/peak timing gaps. Timing score [0-1] from entry gap (60%) + peak gap (40%) with late penalty. Session-aware (TOKYO/LONDON/NEW_YORK/SYDNEY). Scan interval recommendation [60s-900s]: early signals → slow down, poor quality → speed up. First module to optimize when to scan, not just what to scan.
- [2026-03-20] **arch/phase18_complete**: Phase 18 (US-109 through US-114) all 6 stories complete. 67 total automation modules. Theme: adaptive quality — shifted from "run everything" to "run smart with calibration". Execution cost profiling, adaptive gates, dynamic risk sizing, model calibration auditing, 3-way interaction matrices, and scan timing. All files pass py_compile.

### Phase 19: Production Hardening & Orchestration
- [2026-03-20] **feature/memory_manager**: US-115. MemoryManager with ManagedCache wrapper for LRU eviction via access order tracking. Log rotation archives old lines, keeps recent N. RSS monitoring from /proc/self/status with resource module fallback. RSS_WARNING_MB=500, RSS_CRITICAL_MB=1000. Default: max 10K lines, keep 5K. Archive to trained_data/archive/. First module to address unbounded memory growth in watch mode.
- [2026-03-20] **feature/module_activation**: US-116. ModuleActivationMap with ModuleRecord per-module state tracking. Configurable run_every_n_cycles for async dispatch. Dependency-aware topological sort in get_modules_to_run(). Auto-detects orphaned modules (initialized but 0 runs after 5 cycles). complete_cycle() logs activation snapshots every 10 cycles. Exposed 22 initialized-but-never-called modules.
- [2026-03-20] **feature/agent_lifecycle**: US-117. AgentLifecycleManager with 4 status levels: ACTIVE (1.0x), QUARANTINED (0.30x), CANDIDATE (0.50x), RETIRED (0.0x). Quarantine at <35% accuracy, recovery at >50%, retirement at <25% in quarantine. Candidate promotion at >60% after 30 trades. Automatic status evaluation on every fitness update. First module to manage agent lifecycle beyond simple accuracy tracking.
- [2026-03-20] **feature/health_registry**: US-118. HealthRegistry with ModuleHealth per-module check tracking. Critical modules (engine, agents, execution, etc.) weighted 2x in health score. Auto-disable after 3 consecutive failures (non-critical only). MIN_HEALTH_TO_CONTINUE=0.40. Graceful degradation: disabled modules don't block scanning. Preflight/postflight separation. First system-wide health gate.
- [2026-03-20] **feature/observation_consumer**: US-119. ObservationConsumer reads observations.jsonl (97+ observation types that were written but never consumed). Pattern detection at 3 levels: type, pair-specific, regime-specific. 5 recommendation mappings: near_miss → lower threshold, agent_disagreement → increase uncertainty weight, correlation_conflict → tighten filter, regime_change → reduce position size, group_momentum → raise gate. Spike alerting: 5+ = warning, 10+ = critical.
- [2026-03-20] **feature/replay_validator**: US-120. ReplayValidator with simplified momentum-based signal simulator for offline replay. ATR computation from candle data. SL/TP simulation walking forward through future candles. Drift detection: 10% accuracy drop from baseline = alert. A/B config comparison with weighted scoring (Sharpe 40%, win_rate 30%, PnL 30%). First module enabling offline validation of live scan logic.
- [2026-03-20] **arch/phase19_complete**: Phase 19 (US-115 through US-120) all 6 stories complete. 73 total automation modules. Theme: production hardening — addressed 6 critical gaps: unbounded memory, 22 orphaned modules, no agent lifecycle management, no health monitoring, 97 unused observations, no replay validation. System now has memory management, module activation tracking, agent lifecycle with quarantine/promotion, orchestrator health checks, observation consumption, and historical replay. All files pass py_compile.

### Phase 20: Module Wiring & Live Integration
- [2026-03-22] **feature/health_preflight_wiring**: US-121. HealthRegistry wired into engine.py scan loop: run_preflight() before scan logic, should_continue_cycle() aborts on low health score, run_postflight() after execution. _register_health_checks() registers core components as critical (2x weight) and Phase 18/19 modules as non-critical. save_state() at cycle end. Guarded by enable_health_registry flag.
- [2026-03-22] **feature/agent_lifecycle_wiring**: US-122. AgentLifecycleManager wired into two points: (1) continuous.py RL sync — update_fitness(agent_name, won, regime) for each agent vote after trade close, (2) agents/_team.py vote aggregation — get_weight_modifier(agent_name) applied as multiplier before weighted_vote_score. Quarantined agents get 0.30x, retired get 0.0x. Status transitions logged to observation_log.
- [2026-03-22] **feature/observation_consumer_wiring**: US-123. ObservationConsumer wired into continuous.py every 10 cycles: consume_observations() → detect_patterns() → recommend_adjustments() fed to ConfigTuner → check_alerts() logged as warnings. Closes the write-but-never-read gap for 97+ observation types.
- [2026-03-22] **feature/module_dispatcher**: US-124. New ModuleDispatcher class (module_dispatcher.py, 274 lines). Central scheduler: 24 modules registered with frequencies from 1 (memory_manager, health_registry) to 20 (replay_validator, training_augmenter). register_all_modules() auto-discovers from Scanner attributes. execute_cycle() runs scheduled subset each cycle. Dispatch log every 10 cycles. Wired into continuous.py main loop.
- [2026-03-22] **feature/drift_monitor**: US-125. New DriftMonitor class (drift_monitor.py, 316 lines). Bridges ReplayValidator with config_tuner and threshold_optimizer. run_drift_check() → replay_scan() + detect_drift(). Mild drift (>10%): tighten confidence +0.01, notify config_tuner. Critical drift (>20%): force position sizing to 60%, tighten confidence +0.03, raise alert. get_size_multiplier(pair) returns [0.60, 1.0]. Per-pair drift history. Runs every 20 cycles via module_dispatcher.
- [2026-03-22] **feature/memory_manager_wiring**: US-126. MemoryManager wired into continuous.py: run_cleanup() every cycle (LRU eviction + log rotation). RSS monitoring every 10 cycles with rss_warning/rss_critical boolean checks. Emergency cache clear on critical RSS. 5 caches registered (cached_candles, feature_snapshots, raw_snapshots, pair_returns, plus extra). 3 log files registered (observations.jsonl, trade_journal_rl.json, scan_cycle_log.json). Fixed bug: was checking non-existent 'pressure' field, now uses rss_warning/rss_critical booleans.
- [2026-03-22] **arch/phase20_complete**: Phase 20 (US-121 through US-126) all 6 stories complete. 75 total automation modules (73 prior + module_dispatcher + drift_monitor). Theme: wiring — closed the build-vs-wire gap. Phase 19 created 6 production hardening modules that were initialized but NEVER CALLED during scan cycles. Phase 20 wired all 6 into the live loop: health preflight/postflight, agent lifecycle fitness + vote weights, observation consumption feedback, module dispatch scheduling, drift auto-correction, and memory cleanup. All files pass py_compile.

### Phase 21: Execution Unblock & Adaptive Tuning
- [2026-03-22] **feature/threshold_optimizer_feedback**: US-127. ThresholdOptimizer enabled in smart profile. optimize_thresholds() called in scan loop after PairAnalysis built, can re-qualify blocked pairs if optimizer loosened threshold. Feeds optimized values to ConfigAdjuster for persistence. Falls back to defaults with <15 trades.
- [2026-03-22] **feature/atr_regime_fallback**: US-128. ATR-based regime fallback when TCN returns UNKNOWN. Computes ATR percentile from last 100 candles: <25th=LOW, 25-50=NORMAL, 50-75=HIGH, >75=EXTREME. Logs to observation_log with category regime_fallback. Only fires when regime_name=='UNKNOWN'.
- [2026-03-22] **feature/trade_block_logging**: US-129. Trade block reason logging for every gate failure. Filters by confidence>0.30 and direction!=HOLD. Logs each failed gate (confidence, momentum, risk, volatility, consensus) with actual values vs thresholds. Metadata includes full gate status dict.
- [2026-03-22] **feature/regime_consensus**: US-130. Regime-specific agent consensus thresholds: LOW=0.50, NORMAL=0.33, HIGH=0.25, EXTREME=0.20. Looks up threshold by analysis.volatility_regime. Falls back to config.min_agent_consensus_ratio for UNKNOWN. Guarded by enable_regime_consensus flag.
- [2026-03-22] **feature/config_adjuster**: US-131. ConfigAdjuster class (200 lines) — central config adjustment manager. collect_adjustment(), apply_adjustments(), save_state(). Rate-limits 1 change/key/10 cycles. Persists to .claude/config_adjustments.json. Initialized in engine.py, applied every 10 cycles in continuous.py.
- [2026-03-22] **feature/extreme_regime_policy**: US-132. EXTREME regime execution policy: lowers effective_min confidence by 5pts, re-qualifies blocked pairs if risk_passed, applies 0.50x size multiplier via result.risk_pct. Config flags: enable_extreme_regime_policy, extreme_regime_size_multiplier, extreme_regime_confidence_offset.
- [2026-03-22] **arch/phase21_complete**: Phase 21 (US-127 through US-132) all 6 stories complete. 76 total automation modules (+config_adjuster). Theme: execution unblocking — system had 10+ scans finding 0.70+ ensemble scores but only 1 trade. Root causes: ThresholdOptimizer disabled, regime UNKNOWN, gates too tight. Phase 21 enabled adaptive feedback, fallback regime detection, and extreme regime trading.

### Phase 22: Feedback Loop Completion
- [2026-03-22] **feature/threshold_optimizer_persist**: US-133. ThresholdOptimizer save_state() already handled by _save_phase18_state() at engine.py line ~3108 which calls save_state() after outcome recording. Guarded by try/except. Verified existing wiring sufficient.
- [2026-03-22] **feature/phase18_modules_enabled**: US-134. All 4 Phase 18 analytics modules enabled in smart profile: enable_dynamic_risk_allocation=True, enable_model_calibration=True, enable_pair_regime_agent_matrix=True, enable_signal_timing=True. Each module init in engine.py guarded by try/except. Trade outcomes now feed complete analytics.
- [2026-03-22] **feature/risk_pct_passthrough**: US-135. Two-part fix: (1) execution.py already had risk_pct override code reading from analysis_context. (2) Added risk_pct to analysis_context dict builder in engine.py so PairAnalysis.risk_pct (modified by EXTREME regime policy) flows through to execution. Falls back to config.risk_per_trade_pct when None/0.
- [2026-03-22] **feature/journal_replay_warmstart**: US-136. ThresholdOptimizer.replay_from_journal() (~70 lines). Reads trade_journal_rl.json, extracts regime, confidence, R:R from SL/TP distances, momentum from gates. Calls update_outcome() for each trade. Wired in engine.py init: replays journal if _total_outcomes==0. Enables warm-start from historical data.
- [2026-03-22] **feature/orchestrator_config_adjuster**: US-137. ConfigAdjuster initialized in orchestrator._init_modules(). Step 8c in run_cycle() applies pending adjustments via apply_adjustments(scanner.config, cycle_count) and calls save_state(). Added to modules dict in get_system_status(). State exposed in status output.
- [2026-03-22] **feature/graceful_shutdown**: US-138. Shutdown persistence block after while loop exits in continuous.py. Iterates over 5 modules (config_adjuster, threshold_optimizer, agent_lifecycle, observation_consumer, memory_manager), checks scanner attrs first then self attrs, calls save_state(). Each module wrapped in individual try/except.
- [2026-03-22] **arch/phase22_complete**: Phase 22 (US-133 through US-138) all 6 stories complete. 76 total automation modules. Theme: feedback loop completion — closed every remaining gap between module creation and module integration. ThresholdOptimizer persists and warm-starts, Phase 18 analytics fully enabled, regime-adjusted sizing flows end-to-end, orchestrator applies config adjustments, graceful shutdown saves all state. System now truly learns and adapts across sessions.

### Phase 23: Trade Execution Acceleration
- [2026-03-23] **feature/soft_consensus_gate**: US-139. Converted agent consensus from hard gate to confidence penalty. When enable_soft_consensus_gate=True and consensus_ratio >= 0.10 absolute minimum, applies 0.85x confidence multiplier instead of blocking. Below 0.10 still hard-blocks. Dramatically increases trade frequency while preserving safety floor.
- [2026-03-23] **feature/regime_default_normal**: US-140. When ATR fallback still produces UNKNOWN regime, defaults to NORMAL. Downstream regime_consensus_map finds 0.33 threshold instead of falling through. Configurable via volatility_regime_default.
- [2026-03-23] **feature/sub_inference_penalty_soften**: US-141. Sub-inference opposite-direction penalty reduced from 0.20x to 0.50x (configurable). Increases opposite-direction score contribution, reducing vote nullification that was killing otherwise valid signals.
- [2026-03-23] **feature/execution_fasttrack**: US-142. High-quality blocked trades can bypass consensus gate: agent_score >= 0.70 + confidence >= 0.50 + risk_passed. Applies 0.75x risk_pct for safety. Logged as 'execution_fasttrack' observations.
- [2026-03-23] **feature/immediate_gate_adjustments**: US-143. ObservationConsumer.get_immediate_adjustments() produces real-time gate overrides. Three patterns: consensus+disagreement → lower vote threshold, near-miss → lower min_confidence, regime flood → loosen consensus. Rate-limited 1 change/key/5 cycles.
- [2026-03-23] **feature/execution_summary_dashboard**: US-144. Per-cycle execution summary in continuous.py: total→directional→gates✓→agents✓→tradeable pipeline. Shows gate failure breakdown, top blocker, near-miss count, regime distribution.
- [2026-03-23] **arch/phase23_complete**: Phase 23 (US-139 through US-144) all 6 stories complete. 77 total automation modules. Theme: trade execution acceleration — system was executing <1 trade per 10 scans despite finding good signals. Root causes: hard consensus gate, UNKNOWN regime default, harsh sub-inference penalty. Phase 23 softened gates, added fast-track, and provided execution visibility.

### Phase 24: Trade Quality & RL Integrity
- [2026-03-23] **feature/regime_at_exit**: US-145. regime_at_exit captured during trade close sync. _get_current_regime() derives from scanner cached results, falls back to config default, then entry regime. RL system can now differentiate outcomes by volatility transition.
- [2026-03-23] **feature/rl_weight_persistence**: US-146. Fixed ScannerAgentTeam initialization during RL sync — uses scanner.config instead of None. save_learned_weights() already called inside update_weights_from_outcome(). Added before/after weight change logging. Weights no longer lost after computation.
- [2026-03-23] **feature/position_manager_wired**: US-147. LivePositionManager wired into continuous.py every 3 cycles. Builds PositionContext from monitor_open_trades(), evaluates each position. TIGHTEN_SL calls modify_sl_oanda(), EXIT_EARLY calls close_trade_oanda(). Added both OANDA helper methods to ExecutionManager.
- [2026-03-23] **feature/slippage_sizing_feedback**: US-148. Rolling avg slippage per pair (20-trade window). When avg > 2.5 pips, reduces position size by 15%. Minimum 5 trades before activation. Fed to observation_log category='slippage_feedback'.
- [2026-03-23] **feature/trailing_stop_learning**: US-149. Exit-type-aware RL weight updates. Profitable trailing stop exits get 0.05 penalty (vs 0.15 default) — trailing stops are designed to capture profit, not signal failure. Losing trailing exits get normal penalty.
- [2026-03-23] **feature/exit_accountability**: US-150. Agent accountability by exit type in learning_engine. Three patterns: exit_accountability_positive (voted YES, trade won), agent_conservative_miss (voted NO, fast-tracked trade won), exit_accountability_correct_reject (voted NO, trade lost). Enables targeted agent weight improvement.
- [2026-03-23] **arch/phase24_complete**: Phase 24 (US-145 through US-150) all 6 stories complete. 78 total automation modules. Theme: trade quality & RL integrity — fixed critical bugs preventing agent learning (weights lost after computation, regime_at_exit always NULL). Wired position_manager for active management. Added slippage→sizing feedback and exit-type-aware learning. System now trades more (Phase 23) AND learns from outcomes (Phase 24).

### Phase 25: System Hardening & Closed-Loop Activation
- [2026-03-23] **feature/lifecycle_quarantine_voting**: US-151. Agent lifecycle quarantine applied to vote weights — QUARANTINED 0.3x, RETIRED 0x. Changed enable_agent_lifecycle default to True. Enhanced per-agent modifier logging.
- [2026-03-23] **feature/correlation_execution_gate**: US-152. Real-time correlation enforcement in execute_trade() — blocks >0.80 correlated open positions via static _CORR_PAIRS map for 10 high-correlation pair groups. Logs to observation_log.
- [2026-03-23] **feature/threshold_optimizer_enabled**: US-153. ThresholdOptimizer auto-enabled (default True). Added step 3c in continuous.py — reads journal, extracts gate values, calls update_outcome() per trade. optimize_thresholds() per regime every 10 cycles.
- [2026-03-23] **feature/learnings_consolidation**: US-154. Automated learnings consolidation pipeline in orchestrator every 50 cycles. Archives entries >30 days, prunes config_adjustments >100 entries older than 30 days.
- [2026-03-23] **feature/accuracy_matrix_enabled**: US-155. Agent accuracy matrix enabled (default True). Wired record_verdict() into sync_closed_trades_rl(). Injected _accuracy_matrix into ScannerAgentTeam for per-pair weight boosts/penalties.
- [2026-03-23] **feature/stale_weight_decay**: US-156. Stale agent weight decay — 0.95x every 50 trades for agents with no trades in last 20. Floor of 0.5. Prevents dead-weight agents from dragging consensus.
- [2026-03-23] **arch/phase25_complete**: Phase 25 (US-151 through US-156) all 6 stories complete. 78 total automation modules. Theme: system hardening — activated 3 disabled modules (lifecycle, threshold_optimizer, accuracy_matrix), added correlation execution gate, learnings consolidation, stale weight decay.

### Phase 26: Observability & State Integrity
- [2026-03-23] **feature/portfolio_risk_alerting**: US-157. Portfolio risk gates log to observation_log category='portfolio_risk_block' with projected_risk_pct, NAV, exposure breakdown.
- [2026-03-23] **feature/agent_degradation_zscore**: US-158. Agent degradation early-warning via binomial z-score (10-trade recent vs 30-trade baseline). Alerts at z < -1.5. Wired every 5 cycles.
- [2026-03-23] **feature/weight_ema_smoothing**: US-159. EMA-based weight oscillation damping (0.7/0.3 blend). Per-agent weight_history variance tracking. High variance → stronger damping (0.6/0.4).
- [2026-03-23] **feature/json_state_validation**: US-160. Schema validation for agent_weights and trade_journal in safe_json_read(). Validators registered by filename. On schema fail, tries .bak then defaults.
- [2026-03-23] **feature/threshold_drift_velocity**: US-161. Gate threshold drift velocity monitoring. Tracks per-gate change history (last 20), computes velocity over last 3 changes. Alerts when > 0.01/cycle.
- [2026-03-23] **feature/correlation_block_enrichment**: US-162. Enhanced correlation block with blocking_pair_detail (P/L, units, open_time). Tracks conflict frequency by pair combination.
- [2026-03-23] **arch/phase26_complete**: Phase 26 (US-157 through US-162) all 6 stories complete. Theme: observability & state integrity — operators can now see WHY trades blocked, agents degrading, weights oscillating, thresholds drifting. JSON state corruption auto-recovers.

### Phase 27: Feedback Loop Completion
- [2026-03-23] **feature/observation_consumer_enabled**: US-163. ObservationConsumer enabled (default True). Wired every 10 cycles in continuous.py — consumes, detects patterns, recommends adjustments, logs alerts.
- [2026-03-23] **feature/health_registry_enabled**: US-164. HealthRegistry enabled (default True). Health scores in orchestrator get_system_status(). Alerts on score < 0.70.
- [2026-03-23] **feature/execution_efficiency_trending**: US-165. Per-pair execution efficiency tracking — logs high_slippage observation when US-148 reduction triggers.
- [2026-03-23] **feature/fasttrack_ab_logging**: US-166. Fast-track A/B logging — tags fast-track trades, tracks wins/total in execution manager, logs A/B stats every 5 fast-track trades.
- [2026-03-23] **feature/pram_enabled**: US-167. pair_regime_agent_matrix enabled (default True). Fed outcomes from sync_closed_trades_rl() for 3-way interaction tracking.
- [2026-03-23] **feature/manual_override_detection**: US-168. Manual trade override detection in orchestrator. Compares OANDA trades vs journal. Unmatched = manual overrides. Logged to observation_log.
- [2026-03-23] **arch/phase27_complete**: Phase 27 (US-163 through US-168) all 6 stories complete. Theme: feedback loop completion — enabled ObservationConsumer, HealthRegistry, pair_regime_agent_matrix. Added execution efficiency trending, fast-track A/B logging, manual override detection.

### Archived 2026-03-24
- [2026-03-24] [PROMOTED] **SYSTEMIC/phantom_wiring_pattern**: Ralph marks phases "passes: true" with 1000+ unit tests, but 6 out of 6 audited Phase 43-47 modules had broken or missing live wiring. Root cause: unit tests mock integration boundaries, so the module works in isolation but the call site in production is missing, misconfigured, or silently skipped. **10 fixes across 5 files**: (1) confidence_calibration.py — dict vs object mismatch on `.score`/`.weight`, (2) config.py — missing `mtf_confluence_enabled`/`ensemble_conflict_enabled` dataclass fields caused `apply_profile` to silently skip them, (3) regime_detector.py — `percentileofscore()` on uniform data returns 100→EXTREME, (4) execution.py — EWMA correlation 3 methods defined but never called, (5) _team.py — ExpectancyTracker records data but `get_weight_modifier()` never wired into `evaluate()`, (6) execution.py — adaptive exit signals computed but `_act_on_exit_signals()` never existed. **Promotion candidate** (6 observations, same session). Pattern: every time Ralph wires a module, a live scan smoke test must verify the module's log output appears in production.
- [2026-03-24] [PROMOTED] **SYSTEMIC/dataclass_field_gap**: When `ScannerConfig.apply_profile()` validates keys against `__dataclass_fields__` and skips unknowns, profile dict entries for new features are silently dropped. The profile dict, the consumer `getattr()`, and the dataclass definition must all agree. Missing any one of the three = dead feature. Always add the dataclass field FIRST, then the profile entry, then the consumer.
- [2026-03-24] [PROMOTED] **SYSTEMIC/write_only_telemetry**: ExpectancyTracker and EWMA correlation were wired on the write side (execution.py records data) but not the read side (_team.py/engine.py never consumed it). Pattern: any feedback system needs both a writer AND a reader verified in the same PR. If `record_X()` exists without a corresponding `get_X()` call site, the module is dead.
