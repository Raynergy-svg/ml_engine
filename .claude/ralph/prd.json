{
  "branchName": "ralph/buddy-gap-resolution-phase3",
  "description": "Auto-generated gap resolution PRD. Source: prd_aura_phase2.json. Detected 138 integration gaps across 4 categories. DIRECTIVE: NEVER STOP. DONT ASK. ONLY ASK WHEN DESTRUCTIVE.",
  "project": "ML_Engine",
  "userStories": [
    {
      "acceptanceCriteria": [
        "Add self.state_engine.save_state() to graceful shutdown in continuous.py",
        "Add self.memory_manager.save_state() to graceful shutdown in continuous.py",
        "Add self.pair_transfer.save_state() to graceful shutdown in continuous.py",
        "Add self.threshold_optimizer.save_state() to graceful shutdown in continuous.py",
        "Add self.regime_broadcaster.save_state() to graceful shutdown in continuous.py",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules have save_state() methods but aren't called during shutdown. State is lost between sessions.",
      "id": "US-001",
      "notes": "",
      "passes": false,
      "priority": 1,
      "title": "Wire save_state() into graceful shutdown for: state_engine, memory_manager, pair_transfer, threshold_optimizer, regime_broadcaster"
    },
    {
      "acceptanceCriteria": [
        "Add self.agent_accuracy_matrix.save_state() to graceful shutdown in continuous.py",
        "Add self.health_registry.save_state() to graceful shutdown in continuous.py",
        "Add self.agent_lifecycle.save_state() to graceful shutdown in continuous.py",
        "Add self.signal_timing.save_state() to graceful shutdown in continuous.py",
        "Add self.model_calibration.save_state() to graceful shutdown in continuous.py",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules have save_state() methods but aren't called during shutdown. State is lost between sessions.",
      "id": "US-002",
      "notes": "",
      "passes": false,
      "priority": 2,
      "title": "Wire save_state() into graceful shutdown for: agent_accuracy_matrix, health_registry, agent_lifecycle, signal_timing, model_calibration"
    },
    {
      "acceptanceCriteria": [
        "Add self.execution_router.save_state() to graceful shutdown in continuous.py",
        "Add self.execution_quality_optimizer.save_state() to graceful shutdown in continuous.py",
        "Add self.training_augmenter.save_state() to graceful shutdown in continuous.py",
        "Add self.model_bandit.save_state() to graceful shutdown in continuous.py",
        "Add self.affinity_portfolio.save_state() to graceful shutdown in continuous.py",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules have save_state() methods but aren't called during shutdown. State is lost between sessions.",
      "id": "US-003",
      "notes": "",
      "passes": false,
      "priority": 3,
      "title": "Wire save_state() into graceful shutdown for: execution_router, execution_quality_optimizer, training_augmenter, model_bandit, affinity_portfolio"
    },
    {
      "acceptanceCriteria": [
        "Add self.position_manager.save_state() to graceful shutdown in continuous.py",
        "Add self.temporal_attention.save_state() to graceful shutdown in continuous.py",
        "Add self.dynamic_risk_allocator.save_state() to graceful shutdown in continuous.py",
        "Add self.execution_quality_tracker.save_state() to graceful shutdown in continuous.py",
        "Add self.attention_feedback.save_state() to graceful shutdown in continuous.py",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules have save_state() methods but aren't called during shutdown. State is lost between sessions.",
      "id": "US-004",
      "notes": "",
      "passes": false,
      "priority": 4,
      "title": "Wire save_state() into graceful shutdown for: position_manager, temporal_attention, dynamic_risk_allocator, execution_quality_tracker, attention_feedback"
    },
    {
      "acceptanceCriteria": [
        "Add self.model_router.save_state() to graceful shutdown in continuous.py",
        "Add self.causal_filter.save_state() to graceful shutdown in continuous.py",
        "Add self.module_activation.save_state() to graceful shutdown in continuous.py",
        "Add self.replay_validator.save_state() to graceful shutdown in continuous.py",
        "Add self.pair_regime_agent_matrix.save_state() to graceful shutdown in continuous.py",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules have save_state() methods but aren't called during shutdown. State is lost between sessions.",
      "id": "US-005",
      "notes": "",
      "passes": false,
      "priority": 5,
      "title": "Wire save_state() into graceful shutdown for: model_router, causal_filter, module_activation, replay_validator, pair_regime_agent_matrix"
    },
    {
      "acceptanceCriteria": [
        "Change enable_trader_readiness_agent default to True in ScannerConfig",
        "Change enable_llm_macro_agent default to True in ScannerConfig",
        "Change enable_llm_trade_analysis default to True in ScannerConfig",
        "Change enable_meta_manager default to True in ScannerConfig",
        "Change enable_equity_harvester default to True in ScannerConfig",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules are wired into the loop but their config flags default to False, so they never activate.",
      "id": "US-006",
      "notes": "",
      "passes": false,
      "priority": 6,
      "title": "Enable config flags for wired modules: trader_readiness_agent, llm_macro_agent, llm_trade_analysis, meta_manager, equity_harvester"
    },
    {
      "acceptanceCriteria": [
        "Register 'dynamic_sl_tp' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trade_explainer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'multi_horizon_fusion' in module_dispatcher or orchestrator._init_modules()",
        "Register 'claude_subprocess' in module_dispatcher or orchestrator._init_modules()",
        "Register 'group_momentum' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-007",
      "notes": "",
      "passes": false,
      "priority": 7,
      "title": "Register modules in dispatcher/orchestrator: dynamic_sl_tp, trade_explainer, multi_horizon_fusion, claude_subprocess, group_momentum"
    },
    {
      "acceptanceCriteria": [
        "Register 'position_timeout' in module_dispatcher or orchestrator._init_modules()",
        "Register 'hermes_daily_brief' in module_dispatcher or orchestrator._init_modules()",
        "Register 'entropy_sizer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'approval_queue' in module_dispatcher or orchestrator._init_modules()",
        "Register 'retrain_trigger' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-008",
      "notes": "",
      "passes": false,
      "priority": 8,
      "title": "Register modules in dispatcher/orchestrator: position_timeout, hermes_daily_brief, entropy_sizer, approval_queue, retrain_trigger"
    },
    {
      "acceptanceCriteria": [
        "Register 'policy_types' in module_dispatcher or orchestrator._init_modules()",
        "Register 'microstructure_regime' in module_dispatcher or orchestrator._init_modules()",
        "Register 'broker_transport' in module_dispatcher or orchestrator._init_modules()",
        "Register 'kelly_portfolio' in module_dispatcher or orchestrator._init_modules()",
        "Register 'hermes_watchdog' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-009",
      "notes": "",
      "passes": false,
      "priority": 9,
      "title": "Register modules in dispatcher/orchestrator: policy_types, microstructure_regime, broker_transport, kelly_portfolio, hermes_watchdog"
    },
    {
      "acceptanceCriteria": [
        "Register 'adaptive_lr' in module_dispatcher or orchestrator._init_modules()",
        "Register 'confidence_calibrator' in module_dispatcher or orchestrator._init_modules()",
        "Register 'capacity_model' in module_dispatcher or orchestrator._init_modules()",
        "Register 'memory_manager' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trade_outcome_predictor' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-010",
      "notes": "",
      "passes": false,
      "priority": 10,
      "title": "Register modules in dispatcher/orchestrator: adaptive_lr, confidence_calibrator, capacity_model, memory_manager, trade_outcome_predictor"
    },
    {
      "acceptanceCriteria": [
        "Register 'outcome_backfill' in module_dispatcher or orchestrator._init_modules()",
        "Register 'pair_transfer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'threshold_optimizer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'pair_model_selector' in module_dispatcher or orchestrator._init_modules()",
        "Register 'reflection_events' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-011",
      "notes": "",
      "passes": false,
      "priority": 11,
      "title": "Register modules in dispatcher/orchestrator: outcome_backfill, pair_transfer, threshold_optimizer, pair_model_selector, reflection_events"
    },
    {
      "acceptanceCriteria": [
        "Register 'background_activity' in module_dispatcher or orchestrator._init_modules()",
        "Register 'post_deploy_critic' in module_dispatcher or orchestrator._init_modules()",
        "Register 'counterfactual_learner' in module_dispatcher or orchestrator._init_modules()",
        "Register 'serial_executor' in module_dispatcher or orchestrator._init_modules()",
        "Register 'event_handlers' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-012",
      "notes": "",
      "passes": false,
      "priority": 12,
      "title": "Register modules in dispatcher/orchestrator: background_activity, post_deploy_critic, counterfactual_learner, serial_executor, event_handlers"
    },
    {
      "acceptanceCriteria": [
        "Register 'regime_broadcaster' in module_dispatcher or orchestrator._init_modules()",
        "Register 'agent_accuracy_matrix' in module_dispatcher or orchestrator._init_modules()",
        "Register 'self_model_state' in module_dispatcher or orchestrator._init_modules()",
        "Register 'health_registry' in module_dispatcher or orchestrator._init_modules()",
        "Register 'portfolio_optimizer' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-013",
      "notes": "",
      "passes": false,
      "priority": 13,
      "title": "Register modules in dispatcher/orchestrator: regime_broadcaster, agent_accuracy_matrix, self_model_state, health_registry, portfolio_optimizer"
    },
    {
      "acceptanceCriteria": [
        "Register 'adjustment_validator' in module_dispatcher or orchestrator._init_modules()",
        "Register 'session_snapshot' in module_dispatcher or orchestrator._init_modules()",
        "Register 'execution_funnel' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trading_events' in module_dispatcher or orchestrator._init_modules()",
        "Register 'alert_manager' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-014",
      "notes": "",
      "passes": false,
      "priority": 14,
      "title": "Register modules in dispatcher/orchestrator: adjustment_validator, session_snapshot, execution_funnel, trading_events, alert_manager"
    },
    {
      "acceptanceCriteria": [
        "Register 'dynamic_hedging' in module_dispatcher or orchestrator._init_modules()",
        "Register 'dynamic_drawdown' in module_dispatcher or orchestrator._init_modules()",
        "Register 'agent_lifecycle' in module_dispatcher or orchestrator._init_modules()",
        "Register 'signal_timing' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trading_event_bus' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-015",
      "notes": "",
      "passes": false,
      "priority": 15,
      "title": "Register modules in dispatcher/orchestrator: dynamic_hedging, dynamic_drawdown, agent_lifecycle, signal_timing, trading_event_bus"
    },
    {
      "acceptanceCriteria": [
        "Register 'chain_memory' in module_dispatcher or orchestrator._init_modules()",
        "Register 'model_calibration' in module_dispatcher or orchestrator._init_modules()",
        "Register 'execution_router' in module_dispatcher or orchestrator._init_modules()",
        "Register 'execution_quality_optimizer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'adversarial_trainer' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-016",
      "notes": "",
      "passes": false,
      "priority": 16,
      "title": "Register modules in dispatcher/orchestrator: chain_memory, model_calibration, execution_router, execution_quality_optimizer, adversarial_trainer"
    },
    {
      "acceptanceCriteria": [
        "Register 'state_reconciler' in module_dispatcher or orchestrator._init_modules()",
        "Register 'supervision_policy_engine' in module_dispatcher or orchestrator._init_modules()",
        "Register 'autonomous_trainer' in module_dispatcher or orchestrator._init_modules()",
        "Register 'supervision_core' in module_dispatcher or orchestrator._init_modules()",
        "Register 'model_manager' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-017",
      "notes": "",
      "passes": false,
      "priority": 17,
      "title": "Register modules in dispatcher/orchestrator: state_reconciler, supervision_policy_engine, autonomous_trainer, supervision_core, model_manager"
    },
    {
      "acceptanceCriteria": [
        "Register 'hermes_digest' in module_dispatcher or orchestrator._init_modules()",
        "Register 'regime_reward' in module_dispatcher or orchestrator._init_modules()",
        "Register 'training_augmenter' in module_dispatcher or orchestrator._init_modules()",
        "Register 'causal_counterfactual' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trade_slots' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-018",
      "notes": "",
      "passes": false,
      "priority": 18,
      "title": "Register modules in dispatcher/orchestrator: hermes_digest, regime_reward, training_augmenter, causal_counterfactual, trade_slots"
    },
    {
      "acceptanceCriteria": [
        "Register 'event_queue' in module_dispatcher or orchestrator._init_modules()",
        "Register 'model_bandit' in module_dispatcher or orchestrator._init_modules()",
        "Register 'ensemble_disagreement' in module_dispatcher or orchestrator._init_modules()",
        "Register 'regime_tracker' in module_dispatcher or orchestrator._init_modules()",
        "Register 'affinity_portfolio' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-019",
      "notes": "",
      "passes": false,
      "priority": 19,
      "title": "Register modules in dispatcher/orchestrator: event_queue, model_bandit, ensemble_disagreement, regime_tracker, affinity_portfolio"
    },
    {
      "acceptanceCriteria": [
        "Register 'position_manager' in module_dispatcher or orchestrator._init_modules()",
        "Register 'briefing_snapshot' in module_dispatcher or orchestrator._init_modules()",
        "Register 'cycle_autonomy' in module_dispatcher or orchestrator._init_modules()",
        "Register 'smart_execution' in module_dispatcher or orchestrator._init_modules()",
        "Register 'accuracy_gate' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-020",
      "notes": "",
      "passes": false,
      "priority": 20,
      "title": "Register modules in dispatcher/orchestrator: position_manager, briefing_snapshot, cycle_autonomy, smart_execution, accuracy_gate"
    },
    {
      "acceptanceCriteria": [
        "Register 'temporal_attention' in module_dispatcher or orchestrator._init_modules()",
        "Register 'orchestrator' in module_dispatcher or orchestrator._init_modules()",
        "Register 'constitution' in module_dispatcher or orchestrator._init_modules()",
        "Register 'dynamic_risk_allocator' in module_dispatcher or orchestrator._init_modules()",
        "Register 'hermes_state' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-021",
      "notes": "",
      "passes": false,
      "priority": 21,
      "title": "Register modules in dispatcher/orchestrator: temporal_attention, orchestrator, constitution, dynamic_risk_allocator, hermes_state"
    },
    {
      "acceptanceCriteria": [
        "Register 'supervision_tick' in module_dispatcher or orchestrator._init_modules()",
        "Register 'validation_stats' in module_dispatcher or orchestrator._init_modules()",
        "Register 'lead_lag_detector' in module_dispatcher or orchestrator._init_modules()",
        "Register 'continuous' in module_dispatcher or orchestrator._init_modules()",
        "Register 'market_impact' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-022",
      "notes": "",
      "passes": false,
      "priority": 22,
      "title": "Register modules in dispatcher/orchestrator: supervision_tick, validation_stats, lead_lag_detector, continuous, market_impact"
    },
    {
      "acceptanceCriteria": [
        "Register 'pair_affinity' in module_dispatcher or orchestrator._init_modules()",
        "Register 'seasonality' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trading_control_plane' in module_dispatcher or orchestrator._init_modules()",
        "Register 'learnings_outcomes' in module_dispatcher or orchestrator._init_modules()",
        "Register 'close_diagnostician' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-023",
      "notes": "",
      "passes": false,
      "priority": 23,
      "title": "Register modules in dispatcher/orchestrator: pair_affinity, seasonality, trading_control_plane, learnings_outcomes, close_diagnostician"
    },
    {
      "acceptanceCriteria": [
        "Register 'deterministic_surgeon' in module_dispatcher or orchestrator._init_modules()",
        "Register 'feature_attention' in module_dispatcher or orchestrator._init_modules()",
        "Register 'supervision_runtime' in module_dispatcher or orchestrator._init_modules()",
        "Register 'hierarchical_risk_parity' in module_dispatcher or orchestrator._init_modules()",
        "Register 'execution_quality_tracker' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-024",
      "notes": "",
      "passes": false,
      "priority": 24,
      "title": "Register modules in dispatcher/orchestrator: deterministic_surgeon, feature_attention, supervision_runtime, hierarchical_risk_parity, execution_quality_tracker"
    },
    {
      "acceptanceCriteria": [
        "Register 'attention_feedback' in module_dispatcher or orchestrator._init_modules()",
        "Register 'brain_caps' in module_dispatcher or orchestrator._init_modules()",
        "Register 'observational_learning' in module_dispatcher or orchestrator._init_modules()",
        "Register 'model_router' in module_dispatcher or orchestrator._init_modules()",
        "Register 'causal_filter' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-025",
      "notes": "",
      "passes": false,
      "priority": 25,
      "title": "Register modules in dispatcher/orchestrator: attention_feedback, brain_caps, observational_learning, model_router, causal_filter"
    },
    {
      "acceptanceCriteria": [
        "Register 'module_activation' in module_dispatcher or orchestrator._init_modules()",
        "Register 'concept_drift' in module_dispatcher or orchestrator._init_modules()",
        "Register 'trigger_registry' in module_dispatcher or orchestrator._init_modules()",
        "Register 'drift_monitor' in module_dispatcher or orchestrator._init_modules()",
        "Register 'crisis_generator' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-026",
      "notes": "",
      "passes": false,
      "priority": 26,
      "title": "Register modules in dispatcher/orchestrator: module_activation, concept_drift, trigger_registry, drift_monitor, crisis_generator"
    },
    {
      "acceptanceCriteria": [
        "Register 'drift_projector' in module_dispatcher or orchestrator._init_modules()",
        "Register 'event_history' in module_dispatcher or orchestrator._init_modules()",
        "Register 'phase_manager' in module_dispatcher or orchestrator._init_modules()",
        "Register 'session_registry' in module_dispatcher or orchestrator._init_modules()",
        "Register 'pair_regime_agent_matrix' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-027",
      "notes": "",
      "passes": false,
      "priority": 27,
      "title": "Register modules in dispatcher/orchestrator: drift_projector, event_history, phase_manager, session_registry, pair_regime_agent_matrix"
    },
    {
      "acceptanceCriteria": [
        "Register 'api_retry' in module_dispatcher or orchestrator._init_modules()",
        "Register 'meta_types' in module_dispatcher or orchestrator._init_modules()",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules exist in automation/ but aren't registered in the dispatcher or orchestrator.",
      "id": "US-028",
      "notes": "",
      "passes": false,
      "priority": 28,
      "title": "Register modules in dispatcher/orchestrator: api_retry, meta_types"
    },
    {
      "acceptanceCriteria": [
        "Wire 'confidence_calibrator' output into observation_consumer or learning_engine",
        "Non-blocking \u2014 never crash on module init failure (wrap in try/except)",
        "Syntax validation passes"
      ],
      "description": "Modules produce data that nothing consumes \u2014 dead-end outputs that should feed into the learning loop.",
      "id": "US-029",
      "notes": "",
      "passes": false,
      "priority": 29,
      "title": "Connect feedback consumers for: confidence_calibrator"
    }
  ]
}