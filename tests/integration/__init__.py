"""Integration tests for ML Engine trading system.

Integration tests require external services (IB Gateway, OANDA, etc.) and are
skipped in CI/sandbox environments where these services are unavailable.
"""
