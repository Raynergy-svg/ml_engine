"""Retired integration tests.

This file previously validated a legacy training stack that has been retired.
Buddy is trained via `main.py train-buddy`.
"""

import unittest

raise unittest.SkipTest("Retired: legacy integration tests removed in TF-only migration")
