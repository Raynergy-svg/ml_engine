"""Retired example.

The legacy example used a different training stack. Buddy training lives in `main.py`.

  python main.py train-buddy --csv market_data/oanda_USD_JPY_M5.csv
"""

if __name__ == "__main__":
    raise SystemExit(
        "This example was retired (TensorFlow-only migration). Use: python main.py train-buddy"
    )
