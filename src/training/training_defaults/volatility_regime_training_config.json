{
  "schema_version": 1,
  "head": "volatility_regime",
  "model_class": "tcn",
  "description": "TCN volatility regime classifier (4 classes: LOW/NORMAL/HIGH/EXTREME). Architecture details (dilations, kernel sizes, channel widths) stay hardcoded. Operator tunes labels + top-3 HPs (epochs, learning_rate, dropout).",
  "label_generation": {
    "lookahead_bars": 12,
    "sl_atr_mult": 1.0,
    "tp_atr_mult": 1.5,
    "label_mode": "real",
    "winsorize_bounds": [-0.02, 0.02]
  },
  "training_behavior": {
    "enable_per_pair_finetune": false,
    "enable_joint_training": true,
    "min_real_labels_per_pair": 200,
    "expected_r2_band": [0.0, 0.50]
  },
  "hyperparameters": {
    "epochs": 60,
    "learning_rate": 0.001,
    "dropout": 0.3
  },
  "ranges": {
    "lookahead_bars": [4, 48],
    "sl_atr_mult": [0.5, 3.0],
    "tp_atr_mult": [0.8, 4.0],
    "label_mode": ["real", "pseudo", "blend"],
    "epochs": [10, 200],
    "learning_rate": [1e-5, 1e-2],
    "dropout": [0.0, 0.7],
    "min_real_labels_per_pair": [50, 5000]
  }
}
