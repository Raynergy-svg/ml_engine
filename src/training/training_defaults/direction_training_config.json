{
  "schema_version": 1,
  "head": "direction",
  "model_class": "transformer",
  "description": "Tiny Transformer direction head (d_model=16, heads=2, layers=1) plus EMA + EWC + replay. Architecture details stay hardcoded; this file exposes label generation, training behavior, and the top-3 HPs operators may tune.",
  "label_generation": {
    "lookahead_bars": 12,
    "sl_atr_mult": 1.0,
    "tp_atr_mult": 1.5,
    "label_mode": "real",
    "winsorize_bounds": [-0.02, 0.02]
  },
  "training_behavior": {
    "enable_per_pair_finetune": true,
    "enable_joint_training": true,
    "min_real_labels_per_pair": 200,
    "expected_r2_band": [-0.05, 0.30]
  },
  "hyperparameters": {
    "epochs": 50,
    "learning_rate": 0.0003,
    "dropout": 0.4
  },
  "ranges": {
    "lookahead_bars": [4, 48],
    "sl_atr_mult": [0.5, 3.0],
    "tp_atr_mult": [0.8, 4.0],
    "label_mode": ["real", "pseudo", "blend"],
    "epochs": [10, 200],
    "learning_rate": [1e-5, 1e-2],
    "dropout": [0.0, 0.7],
    "min_real_labels_per_pair": [50, 5000]
  }
}
