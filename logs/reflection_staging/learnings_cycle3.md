### Reflection Cycle #3: 3 New Losses (1243 EUR_GBP, 1255 USD_CHF, 1261 USD_CAD), Models FRESH — 2026-04-16T19:53Z

- [2026-04-16] **CRITICAL/regime_low_sl_mult_still_0.8**: All 3 losses entered LOW regime with `regime_atr_multipliers.LOW.sl_mult=0.8` baked into trade. Trade 1255: SL=8.0p, ATR=7.6p, SL/ATR=1.05x. Trade 1261: SL=7.3p, ATR=9.6p, SL/ATR=0.76x (sub-1-ATR stop, INSIDE noise envelope). Cycle 1 raised `atr_sl_multiplier` BASE to 1.2 but the regime override (config.py:504) is still 0.8 and `enable_regime_atr_adaptation=True` means base value is ignored for LOW. Trading rule (promoted 2026-04-15) explicitly mandates LOW sl_mult≥1.2. **Defensive fix: flip `enable_regime_atr_adaptation=False` until per-regime override is patched.**

- [2026-04-16] **CRITICAL/regime_shift_blew_stops**: All 3 losses had `regime_at_entry=LOW` but `regime_at_exit=NORMAL` — entered during artificial calm window, vol expanded post-fill and tight LOW-regime stops were guaranteed losers. MFE=0p on ALL 3 = price never moved favorably even one tick. Compounding regime-tightened stops with regime-shift exits is a systematic loss factory.

- [2026-04-16] **PATTERN/uncertainty_soft_penalty_insufficient**: All 3 losses recorded `model_disagreement=0.5` with `disagreement_soft_penalty` reason (-0.05 confidence delta). Cycle 1 set `max_model_disagreement=0.25` (history shows applied=1) yet uncertainty agent still soft-penalized at 0.50 instead of hard-blocking. Either: (a) uncertainty agent reads a different threshold, (b) agent code uses a hardcoded 0.50 ceiling for "soft" branch. Investigation needed: grep `disagreement_soft_penalty` in agents/.

- [2026-04-16] **PATTERN/fresh_models_still_misdirecting**: Modular ensemble retrained 0.2d ago (15:29Z today). 3 LIVE-traded predictions on fresh weights = 0/3 wins, MFE=0 on all. Fresh ≠ correct. Either: (a) training set didn't include current regime distribution, (b) val_accuracy of 0.556 reflects a near-coinflip model (2026-04-16 cycle 2 noted 0.556). When fresh models are still ~55% accurate AND every loss has MFE=0, the problem is calibration/direction-quality, not staleness. Recommend: validate that the new ensemble produces inverted-but-correct signals on a held-out window before continuing live execution.

- [2026-04-16] **PATTERN/mean_reversion_dominates_LOW_entries**: 1243 mean_reversion supported SHORT @RSI 70.4. 1255 mean_reversion supported LONG @RSI 40.5. 1261 mean_reversion neutral but score=0.57 still passed. Mean reversion is the primary signal-driver in LOW regime and 2/3 of those are direct losses. Propose: cut mean_reversion weight in LOW regime from 0.9 → 0.65. RL bandit weights file (agent_weights.json) shows LOW.mean_reversion=0.9 and history is empty — manual override needed.

- [2026-04-16] **PATTERN/trend_neutral_at_ADX_54_64_misleading**: Trade 1243 trend agent: `"trend neutral (ADX 64)"`, score=0.72, passed=True. ADX 64 is an EXTREME trend strength reading; classifying it as "neutral" is broken logic. Trend agent appears to evaluate ADX magnitude without checking trade-direction alignment with +DI/-DI dominance. This false-positive trend support contributed to WVS=0.83 on 1243. Same pattern noted in cycle 2 — still unfixed.

- [2026-04-16] **PATTERN/risk_sentinel_overweight_in_consensus**: Risk sentinel scored 1.0 on all 3 trades with weight=0.50-0.57 (highest of any agent). With the WVS formula, risk_sentinel alone contributes ~30% of vote score. Risk sentinel only checks portfolio drawdown, not signal quality — so a fresh account with low DD will ALWAYS get a 1.0 risk_sentinel vote, mechanically pushing WVS above gates. Propose: cap risk_sentinel weight contribution at 0.35 OR exclude it from directional vote (use only as veto).

- [2026-04-16] **OBSERVATION/min_confidence_gate_evaded**: cycle1 set `min_confidence=68.0`. Trade 1243 conf=0.677 (67.7), 1255 conf=0.668 (66.8), 1261 conf=0.641 (64.1) — all BELOW the threshold yet executed. Either `min_confidence` field uses a different scale (0-100 vs 0-1) or `confidence` recorded in journal differs from gate-input scale. Verify: `agents.py` confidence vs `config.min_confidence` units.

- [2026-04-16] **POSITIVE/cycle1_consumer_wiring_works**: ConfigAdjuster._load_state correctly loaded pending (config_adjustments.json shows pending={}, total_adjustments=5, history populated). The cycle 2 dead-letter bug IS fixed. Cycle 3 proposals SHOULD apply on next restart. Verify by checking `last_applied` timestamps post-restart.

- [2026-04-16] **IMMEDIATE_ACTION/halt_or_throttle**: Recommend pause-on-loss policy: after 3 consecutive losses on fresh models, throttle to 25% size for next 5 trades regardless of WVS. Current cycle 1 already cut to risk_per_trade=0.025 (2.5%) — propose tightening to 0.0125 (1.25%) until 3 consecutive WINS unlock normal sizing. Capital-preservation > learning-rate during cold start.

---

### Reflection Cycle #3 (re-trigger) — 2026-04-24T03:14Z

- [2026-04-24] **OPERATIONAL/stale_loss_tail_reanalysis**: Trigger fired on the same 3 trades (1243/1255/1261, all 2026-04-15) — no new closes since 04-21. Re-running analytical proposals would only inflate the same maxed levers. Root cause is unchanged from SELFHEAL-C1 (02:55 + 03:06 entries in reflection_log.jsonl): (a) RL retrain not executed (agent_weights.json mtime still 04-16, age=8d > 7d hard-block threshold), (b) LOW-regime `sl_mult=0.8` baked into entries despite ScannerConfig defaults LOW=1.2 / smart=1.3 — pending JSON adjustments are being shadowed by an in-memory profile or a persisted override, (c) `enable_mean_reversion_veto` + `enable_trend_agent_hard_veto` (Phase 91/93) defined in code but the trades pre-date their activation. No new config_adjustments staged this cycle.
- [2026-04-24] **REQUIRED_ACTION/operational_unblock**: (1) Run RL retrain to refresh `trained_data/models/agent_weights.json` (current 8d > 7d threshold; stale-rule promoted 2026-04-15 mandates uncertainty hard-block at 0.35 while stale — none of the 3 trades would have passed). (2) Verify `apply_profile('smart')` is actually called at boot — grep scanner startup for the LOW sl_mult value in effect. (3) Drain `proposed_weights.json` if any prior cycle wrote it. Until (1) is done, no new live trades should be permitted; until (2) is verified, LOW-regime entries remain mechanically lossy.
