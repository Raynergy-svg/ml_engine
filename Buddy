#!/usr/bin/env bash
set -euo pipefail

# Repo-local launcher for Buddy.
# Usage: ./Buddy --config ./config.yaml [--instrument EUR_USD --granularity M5 --candles 300 --execute]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/main.py" buddy "$@"
